# Suppress npm deprecation warnings
# These warnings come from transitive dependencies (dependencies of dependencies)
# and don't affect functionality. They're safe to ignore.
fund=false
audit=false

