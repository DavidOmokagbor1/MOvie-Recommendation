# ML API Requirements
# Core dependencies (required)
# Versions compatible with Python 3.9-3.11
Flask==2.3.3
Flask-Cors==4.0.0
numpy==1.24.3
scipy==1.10.1
scikit-learn==1.3.0
pandas==2.0.3
tqdm==4.66.1
requests==2.31.0

# Production WSGI server (required for production deployment)
gunicorn==21.2.0

# PyTorch for neural models (NeuralMF, DeepFM) - OPTIONAL
# The API will work with EASE and ItemKNN without PyTorch
# Uncomment the lines below if you want to use neural models:
# torch==1.7.1
# torchvision==0.8.2
# torchaudio==0.7.2

# Note: PyTorch is large (~500MB). For Render free tier, you may want to:
# 1. Use only EASE and ItemKNN models (no PyTorch needed)
# 2. Or upgrade to a paid tier for PyTorch support

